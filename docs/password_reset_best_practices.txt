# Flask route to handle the password reset process
@app.route('/reset_password', methods=['POST'])
def reset_password():
    if 'new_password' in request.form and 'confirm_password' in request.form:
        new_password = request.form['new_password']
        confirm_password = request.form['confirm_password']
        
        # Get user data from session
        first_name = session.get('first_name')
        last_name = session.get('last_name')

        if new_password == confirm_password:
            # Check if password contains the user's first name or last name
            if (first_name and first_name.lower() in new_password.lower()) or \
               (last_name and last_name.lower() in new_password.lower()):
                return render_template('reset_password.html', error_message="Password cannot contain your first name or last name.")

            # Check for additional password policies, such as minimum length, special characters, etc.
            if len(new_password) < 8:
                return render_template('reset_password.html', error_message="Password must be at least 8 characters long.")
            if not re.search(r'[A-Z]', new_password) or not re.search(r'[a-z]', new_password) or not re.search(r'[0-9]', new_password):
                return render_template('reset_password.html', error_message="Password must contain at least one uppercase letter, one lowercase letter, and one digit.")

            # Generate hash for the new password
            hashed_password = generate_password_hash(new_password)

            # Get the email from the session
            email = session.get('user_email')

            # Check if the email exists in the accounts table
            connection = sqlite3.connect("verifications_database.db")
            cursor = connection.cursor()

            cursor.execute("SELECT id FROM accounts WHERE email = ?", (email,))
            user_id = cursor.fetchone()

            if not user_id:
                return {"message": "Invalid email"}, 400

            # Check if the new password matches any of the previous passwords
            cursor.execute("SELECT password_hash FROM password_history WHERE user_id = ? ORDER BY change_timestamp DESC LIMIT 1", (user_id[0],))
            previous_password_hash = cursor.fetchone()

            if previous_password_hash and check_password_hash(previous_password_hash[0], new_password):
                # If the new password matches the previous one, return a proper warning message
                return render_template('reset_password.html', error_message="Please choose a different password. You cannot reuse your previous password.")
            else:
                # Update the password in the 'accounts' table
                cursor.execute("UPDATE accounts SET password = ? WHERE email = ?", (hashed_password, email))
                
                # Insert the updated password hash into the password_history table
                cursor.execute("INSERT INTO password_history(user_id, password_hash, change_timestamp) VALUES (?, ?, ?)",
                               (user_id[0], hashed_password, datetime.datetime.now()))

                connection.commit()
                connection.close()

                return redirect(url_for('login'))
        else:
            return render_template('reset_password.html', error_message="Passwords do not match.")
    else:
        logger.warning('Invalid password reset request.')
        return {"message": "Invalid request"}, 400         so in reality also same table or sepearate table like this in real times how it will be almost like this aor what



==============================================================

Chat-GPT Response:-
In real-world applications, your current code structure for handling password resets is quite close to best practices. However, let's go through how it is done professionally in production systems, including table structures, security, and code design.

✅ 1. Password Storage: accounts Table

Reality:

Yes, storing hashed passwords in the main accounts table is standard practice.

It is typical to store only the current password hash in the accounts table.

Sample structure:

CREATE TABLE accounts (
    id INTEGER PRIMARY KEY,
    email TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    -- other user fields like first_name, last_name, etc.
);

✅ 2. Password History: Separate password_history Table

Reality:

Using a separate password_history table is best practice.

This is especially useful for enforcing rules like "cannot reuse last 5 passwords."

Sample structure:

CREATE TABLE password_history (
    id INTEGER PRIMARY KEY,
    user_id INTEGER NOT NULL,
    password_hash TEXT NOT NULL,
    change_timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES accounts(id)
);

✅ 3. Password Policy Enforcement

In real systems, you should check:

✅ Minimum length (8–12+ characters)

✅ Uppercase, lowercase, digits, special characters

✅ No dictionary words or personal information (like name, email)

✅ Not similar to previous password(s)

✅ Not among common passwords (can use a dictionary or service like Have I Been Pwned
)

You're already doing most of this. You could add:

COMMON_PASSWORDS = {'password', '123456', 'qwerty', ...}
if new_password.lower() in COMMON_PASSWORDS:
    return render_template(..., error_message="Password is too common. Choose a stronger one.")

✅ 4. Hashing Passwords

You're using:

hashed_password = generate_password_hash(new_password)


Perfect! Just make sure you're using Werkzeug's generate_password_hash, which defaults to pbkdf2:sha256, a secure algorithm.

In larger systems, people may switch to Argon2 or bcrypt using libraries like passlib.

✅ 5. Session Usage and Security

You use session.get('user_email') — this is fine as long as:

Sessions are secure (SESSION_COOKIE_SECURE = True)

Sensitive actions (like password reset) have a time limit and verification step (e.g., user clicked on an emailed reset link within X minutes)

✅ 6. Real-World Enhancements
✅ Token-Based Password Reset (Instead of Session Only)

How it works:

User clicks "Forgot password"

Backend creates a secure token linked to user (e.g., JWT or UUID stored in DB)

Sends email with /reset_password/<token>

Token is checked before allowing password reset

This is more secure than relying on session data alone.

✅ Audit Logging (Optional but Important)

Track when and who changed the password:

CREATE TABLE audit_logs (
    id INTEGER PRIMARY KEY,
    user_id INTEGER NOT NULL,
    action TEXT,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);

🔒 Real-World Flow Summary

User requests password reset (form with email)

Backend sends email with tokenized link

User clicks link → token is validated

User sets new password

System:

Validates new password (policy checks)

Ensures it's not reused (check password_history)

Updates accounts.password_hash

Inserts into password_history

Logs the change

🧠 Summary: Your Code vs Real World
Aspect	Your Code	Real-World Practice
Store current password in accounts	✅ Yes	✅ Yes
Store history of old passwords	✅ Yes	✅ Yes
Password policy enforcement	✅ Basic checks	✅ Often more advanced (entropy, blacklist)
Reuse prevention	✅ One previous password	✅ Often last 5 or N passwords
Secure password hashing	✅ Yes	✅ Yes
Session usage for email/name	✅ Works	🔄 Token-based is more secure
Logging	❌ No	✅ Usually added
Password reset link via email	❌ No	✅ Strongly recommended

If you'd like, I can help you refactor this code to use tokenized password reset with expiration, or set up a better password policy module.

  




      
